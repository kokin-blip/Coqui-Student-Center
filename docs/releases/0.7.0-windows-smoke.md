# Coqui Student Center 0.7.0 Windows x64 smoke record

- Artifact: `Coqui Student Center_0.7.0_x64-setup.exe`.
- Size: 16,238,030 bytes.
- SHA-256: `C23CF303EFD04CD182FE15D48EAC3F4A451F06D96ADD506F02630E2770D29AD1`.
- Silent current-user install: passed with exit code 0.
- Installed executable: 31,524,352 bytes with the bundled verified OCR runtime.
- Fresh launch: passed; the `Coqui Student Center` window remained responsive and created a fresh encrypted database/device-key envelope.
- Restart: passed; the installed window reopened against the isolated profile and closed normally.
- Native IPC/first-run smoke: passed; schema version 11, onboarding required, dashboard absent, and no forced `Alex Morgan` data.
- Existing profile safety: the original roaming and WebView directories were moved to an isolated Temp backup before the smoke, then restored to their exact paths after the test.
- Silent uninstall: passed with exit code 0; the installed `Coqui Student Center` directory was removed.
- Uninstall data behavior: the isolated smoke profile was removed explicitly after verification; the user's original encrypted profile was restored.

Apple Silicon packaging and the same native WebdriverIO first-run smoke are configured for the `macos-15` ARM64 CI lane. This Windows host cannot build or execute the DMG.
