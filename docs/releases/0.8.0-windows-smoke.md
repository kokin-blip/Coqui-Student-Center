# 0.8.0 Windows installer smoke record

Host: Windows 11 Home 10.0.26200, x64. Unsigned NSIS build.

Artifact: `Coqui Student Center_0.8.0_x64-setup.exe`
Size: 16,302,722 bytes
SHA-256: `27a17e12fd58b1c33155be7d84981e2713a87f90c344cb7b1797269ee86c5f9e`

## Profile safety

The pre-existing encrypted profile was **moved** (not copied) out of
`%APPDATA%\app.studentcenter.desktop` and `%LOCALAPPDATA%\app.studentcenter.desktop`
before installing, so the smoke test ran against empty storage.

| Checkpoint | `student-center.db` SHA-256 |
|---|---|
| Before the test | `163F3BA5AA5B8827E0B75E07409A3DF3997C15F318B04BAD5B6A231895BF9776` |
| After restore | `163F3BA5AA5B8827E0B75E07409A3DF3997C15F318B04BAD5B6A231895BF9776` |

Identical. The isolated smoke profile and the staging directory were removed.

## Results

| Step | Result |
|---|---|
| Silent install (`/S`) | exit 0, executable reports 0.8.0 |
| Cold first launch | window titled "Coqui Student Center" visible in **1,201 ms**, responding |
| Fresh encrypted profile created | yes |
| Restart | window visible in **581 ms**, responding |
| Silent uninstall (`/S`) | exit 0, install directory removed |
| Real profile restored | yes, hash matches |

Cold start previously spent up to ten seconds in the OCR readiness probes before
the window was created; those now run on a background thread after the window is
shown.

## Native IPC smoke

`npm run test:desktop:e2e` — 2 passing:

- launches the bundled UI and exposes an empty native first run
- finishes setup from a name and timezone alone and returns a usable plan
  (asserts a non-null dashboard and zero courses — the regression that caused the
  permanent "Loading your plan…" screen)

## Not covered

- Apple Silicon packaging still requires the macOS CI lane.
- Signing and notarization remain outstanding.
