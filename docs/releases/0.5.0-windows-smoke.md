# Student Center 0.5.0 Windows x64 smoke record

- Artifact: `Student Center_0.5.0_x64-setup.exe`
- Size: 15,997,138 bytes
- SHA-256: `1CB02CF26AA84718D63F3D3AA32585BB891CD023544712EAC6C71582782291C5`
- Silent install to an isolated smoke directory: passed.
- Installed product version: `0.5.0`.
- Fresh first launch: remained running and responsive after five seconds.
- Restart: remained running and responsive after five seconds.
- Fresh encrypted profile: SQLCipher database and DPAPI-protected device key both created.
- Existing profile safety: the prior encrypted profile was temporarily staged, restored after the fresh-profile test, and its SHA-256 remained byte-identical.
- Silent uninstall: passed; installed executable removed.
- Uninstall data behavior: the encrypted smoke profile remained available for recovery verification.
- Browser-mode production bundle: searchable vault, saved source evidence, and managed-AI sign-in/consent gating passed.

The first packaged attempt exposed a fatal `ERROR_NO_SUCH_LOGON_SESSION` from Windows Credential Manager. The release gate was stopped, startup key protection was changed to a DPAPI-backed authoritative file with safe legacy migration, and the installer was rebuilt before this successful smoke record was produced.

Apple Silicon packaging remains enforced by the `macos-15` ARM64 CI lane; this Windows host cannot execute a DMG locally.
